server:
  port: 8081
spring:
  application:
    name: yuan-auth
  datasource:
        url: jdbc:mysql://localhost:3306/yuan_auth?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
        username: ${MYSQL_USER}
        password: ${MYSQL_PASSWORD}
        driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 172.23.80.90:8848
  data:
    redis:
      host: 172.23.80.90
      port: 6379
      password: Lqy147258369
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl